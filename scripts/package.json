{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "migrate-passwords": "tsx ./src/migrate-passwords.ts",
    "prune-reset-tokens": "tsx ./src/prune-reset-tokens.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify-database": "tsx ./src/verify-database.ts"
  },
  "dependencies": {
    "@workspace/db": "workspace:*",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
